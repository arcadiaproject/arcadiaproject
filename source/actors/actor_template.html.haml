---
layout: html5
---
#left-column
	= link_to '/',:ref=> 'home', :class => "no style" do
		#logo
	%nav{:role => "navigation_menu"}
		%ul
			%li
				%a{:href => "../bestiarum.html"} All
	
#right-column
	.evento
		.title
			%h1 
				#{actor.name}
			%p.date 
				#{actor.date}
		.box
			.white-box
				%p.quote 
					&#171; #{actor.quote1} &#187;; 
			.white-box
				%p.quote
					&#171; #{actor.quote2} &#187;;
			
			.white-box
				%p.quote
					&#171; #{actor.quote3} &#187;;		
					 
					
		
			
		.content= partial "#{actor.url}" 
			
				